@model InvitaShare.ViewModels.EventUserDTO

<h3>Are you sure you want to delete this guest?</h3>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            User Email:
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.UserMail)
        </dd>
    </dl>

    @* <form asp-action="DeleteWeddingEvent">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form> *@
</div>